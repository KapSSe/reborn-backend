'use strict';

/**
 *  section-mission controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::section-mission.section-mission');
